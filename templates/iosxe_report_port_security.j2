<!DOCTYPE html>
<html>
<head>
    <title>Report Port Security</title>
    <style>
        table {
            width: 100%;
            border-collapse: collapse;
        }
        table, th, td {
            border: 1px solid black;
        }
        th, td {
            padding: 15px;
            text-align: center;
        }
        th {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>
<table>
    <thead>
        <tr>
            <th>Hostname</th>
            <th>Interfaces With Port Security Enabled</th>
        </tr>
    </thead>
    <tbody>
{% for device in groups['switches'] %}
        <tr>
            <td>{{ device }}</td>
            <td>
            {% if hostvars[device]['interfaces_data'] is defined %}
                {% for key, value in hostvars[device]['interfaces_data'].items() %}
                    {{ value.keys() }}
                {% endfor %}
            {% endif %}
            </td>
        </tr>
{% endfor %}
    </tbody>
</table>
</body>
</html>